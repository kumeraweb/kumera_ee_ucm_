---
import { CONTACT_INFO, getWhatsAppLink } from '../data/config'
---

<section class="relative overflow-hidden bg-slate-950 text-white">
  <div class="absolute inset-0">
    <div class="absolute inset-0 bg-gradient-to-b from-slate-950 via-slate-950/90 to-slate-900"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 pt-14 pb-12 md:pt-20 md:pb-20">
    <div class="grid gap-10 md:grid-cols-[1.1fr_0.9fr] items-center">
      <div class="space-y-5">
        <div class="eyebrow">
          {CONTACT_INFO.roleLabel}
        </div>

        <h1 class="font-black text-white leading-[1.05] text-[clamp(2rem,6vw,3.4rem)]">
          Tranquilidad y contratacion informada para tu plan
        </h1>

        <p class="text-blue-50/90 text-[clamp(1rem,2.6vw,1.25rem)] max-w-xl">
          Asesoria comercial clara y gestion de contratacion con orientacion paso a paso.
        </p>

        <p class="rounded-2xl border border-white/15 bg-white/10 px-4 py-3 text-xs md:text-sm text-blue-50/90 max-w-xl">
          Este sitio es exclusivamente para asesoria y contratacion de planes. No prestamos servicios operativos ni reemplazamos canales oficiales.
        </p>

        <div class="flex flex-wrap gap-2">
          <span class="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-[11px] font-semibold uppercase tracking-widest text-white/90">Familias</span>
          <span class="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-[11px] font-semibold uppercase tracking-widest text-white/90">Adultos mayores</span>
          <span class="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-[11px] font-semibold uppercase tracking-widest text-white/90">Empresas</span>
        </div>

        <div class="flex flex-col gap-3 sm:flex-row sm:items-center">
          <a href="#planes" class="btn btn-primary w-full sm:w-auto">Ver planes y condiciones</a>
          <a href={getWhatsAppLink()} target="_blank" class="btn btn-secondary w-full sm:w-auto">
            WhatsApp comercial
          </a>
        </div>
      </div>

      <div class="relative">
        <div class="card-strong overflow-hidden">
          <div class="aspect-[4/3] md:aspect-[5/4]">
            <img
              src="/ejecutiva.png"
              alt={`${CONTACT_INFO.roleLabel} - asesoria comercial`}
              class="h-full w-full object-cover"
              loading="eager"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
