---
import { CONTACT_INFO } from '../data/config'
---

<section class="relative overflow-visible bg-blue-950 text-white">
  <div class="absolute inset-0">
    <img
      src="/ejecutiva.png"
      alt=""
      class="h-full w-full object-cover opacity-30"
      aria-hidden="true"
      loading="eager"
    />
    <div class="absolute inset-0 bg-gradient-to-br from-blue-950 via-blue-950/90 to-blue-900/70"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 pt-16 pb-12 md:pt-24 md:pb-28">
    <div class="grid gap-8 md:gap-12 md:grid-cols-[1.1fr_0.9fr] items-center">
      <div>
        <div
          class="inline-flex items-center gap-2 rounded-full bg-white/10 px-4 py-2 text-xs font-bold uppercase tracking-widest text-white shadow-md"
        >
          Ejecutiva Comercial Autorizada
        </div>

        <h1 class="mt-5 text-4xl md:text-5xl lg:text-6xl font-black leading-tight">
          Tu Salud y la de tus Padres Asegurada
        </h1>

        <p class="mt-4 text-lg md:text-xl font-medium text-blue-50 max-w-xl">
          Ejecutiva Autorizada. Gestión directa sin call centers.
        </p>

        <p class="mt-3 text-blue-100 max-w-xl">
          Servicio de Rescate Médico Privado con planes mensuales claros para familias y empresas.
        </p>

        <div class="mt-4 flex flex-wrap gap-2 text-xs font-bold uppercase tracking-widest text-blue-100">
          <span class="rounded-full bg-white/10 px-3 py-1 shadow-sm">Adultos mayores</span>
          <span class="rounded-full bg-white/10 px-3 py-1 shadow-sm">Familias</span>
          <span class="rounded-full bg-white/10 px-3 py-1 shadow-sm">Empresas</span>
          <span class="rounded-full bg-white/10 px-3 py-1 shadow-sm">
            Cobertura Valparaíso y Metropolitana
          </span>
        </div>

        <a
          href="#planes"
          class="mt-6 inline-flex w-full min-h-[56px] items-center justify-center rounded-3xl bg-red-600 px-6 text-base font-black uppercase tracking-wide text-white shadow-2xl shadow-red-700/30 transition-transform hover:-translate-y-0.5 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-400 sticky bottom-4 z-40 md:static md:w-auto"
        >
          Ver precios y planes 👇
        </a>
      </div>

      <div class="relative md:pl-6 md:-mr-6">
        <div class="absolute -inset-6 rounded-3xl bg-white/10 shadow-2xl blur-sm"></div>
        <div class="relative -mx-4 rounded-none bg-white/10 p-3 shadow-2xl md:mx-0 md:translate-y-6 md:rounded-3xl">
          <img
            src="/ejecutiva.png"
            alt={`${CONTACT_INFO.name} Ejecutiva Comercial Autorizada`}
            class="w-full rounded-none object-cover md:min-h-[440px] md:rounded-3xl"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </div>
</section>
