---
import { PLANES } from '../data/planes'
import { getWhatsAppLink } from '../data/config'
import { CheckCircle2 } from 'lucide-astro'
---

<section id="planes" class="relative z-20 bg-slate-50 py-12 md:-mt-20 md:py-20">
  <div class="max-w-7xl mx-auto px-4">
    <div class="text-center mb-10">
      <h2 class="text-3xl md:text-4xl font-black text-slate-900">Planes a tu medida</h2>
      <p class="mt-3 text-lg text-slate-600">
        Te oriento para elegir el plan correcto según tu edad, familia o empresa.
      </p>
    </div>

    <div class="grid gap-8 md:grid-cols-2 max-w-5xl mx-auto">
      {
        PLANES.map((plan) => (
          <div
            class={`relative rounded-3xl bg-white p-8 border-2 shadow-2xl ${
              plan.id === 'senior'
                ? 'border-amber-400 ring-2 ring-amber-200/60 md:translate-y-3'
                : 'border-blue-900/20'
            }`}
          >
            {plan.etiqueta && (
              <div class="absolute -top-4 left-1/2 -translate-x-1/2 rounded-full bg-red-600 px-4 py-1 text-sm font-bold uppercase tracking-wide text-white shadow-lg">
                {plan.etiqueta}
              </div>
            )}

            <h3 class="text-3xl md:text-4xl font-black text-slate-900 mb-2">{plan.nombre}</h3>

            <div class="flex items-baseline mb-6">
              <span class="text-sm text-slate-500 font-semibold mr-1">Desde</span>
              <span class="text-5xl font-black text-blue-900">{plan.precio}</span>
              <span class="text-sm text-slate-500 font-bold ml-1">{plan.unidad}</span>
            </div>

            <ul class="space-y-4 mb-8">
              {plan.features.map((feature) => (
                <li class="flex items-start gap-3">
                  <CheckCircle2 class="w-7 h-7 text-green-500 shrink-0" />
                  <span class="text-slate-700 font-medium text-base">{feature}</span>
                </li>
              ))}
            </ul>

            <a
              href={getWhatsAppLink()}
              target="_blank"
              class="block w-full min-h-[48px] rounded-3xl bg-red-600 py-4 text-center text-lg font-bold text-white shadow-lg shadow-red-700/30 transition-transform hover:-translate-y-0.5 hover:bg-red-700"
            >
              Solicitar este Plan
            </a>
          </div>
        ))
      }
    </div>

    <div class="mt-12 grid gap-6 md:grid-cols-3">
      <div class="rounded-3xl border border-slate-100 bg-white p-6 shadow-lg">
        <p class="text-sm font-bold uppercase tracking-widest text-blue-900">Mini FAQ</p>
        <h3 class="mt-2 text-xl font-black text-slate-900">¿Es para adultos mayores?</h3>
        <p class="mt-2 text-slate-600">
          Sí. Hay planes pensados para personas mayores y sus familias.
        </p>
      </div>

      <div class="rounded-3xl border border-slate-100 bg-white p-6 shadow-lg">
        <p class="text-sm font-bold uppercase tracking-widest text-blue-900">Mini FAQ</p>
        <h3 class="mt-2 text-xl font-black text-slate-900">¿Qué necesito para contratar?</h3>
        <p class="mt-2 text-slate-600">
          Solo datos básicos. La ejecutiva autorizada guía todo el proceso.
        </p>
      </div>

      <div class="rounded-3xl border border-slate-100 bg-white p-6 shadow-lg">
        <p class="text-sm font-bold uppercase tracking-widest text-blue-900">Mini FAQ</p>
        <h3 class="mt-2 text-xl font-black text-slate-900">¿Cuánto demora?</h3>
        <p class="mt-2 text-slate-600">
          En pocos minutos puedes tener tu plan listo y activo.
        </p>
      </div>
    </div>
  </div>
</section>
