---
import { CONTACT_INFO, getWhatsAppLink } from '../data/config'
---

<section class="py-16 bg-[#0F1F3D] text-white">
  <div class="max-w-5xl mx-auto px-4">
    <div class="relative overflow-hidden rounded-3xl border border-white/10 bg-gradient-to-r from-blue-900/70 via-blue-900/50 to-blue-800/40 p-8 md:p-12">
      <div class="absolute -right-10 -top-10 h-40 w-40 rounded-full bg-blue-500/20 blur-3xl"></div>
      <h2 class="text-3xl md:text-4xl font-black">Hablemos para cotizar</h2>
      <p class="mt-3 text-lg text-blue-100">
        Soy {CONTACT_INFO.roleLabel}. Brindo asesoria comercial y gestion de contratacion.
      </p>
      <div class="mt-6 flex flex-col gap-4 sm:flex-row">
        <a
          href={`tel:${CONTACT_INFO.phone}`}
          class="flex-1 rounded-2xl bg-white px-6 py-4 text-center text-lg font-black text-blue-900 shadow-lg shadow-blue-900/30 hover:bg-slate-100"
          aria-label="Consulta comercial"
        >
          Consulta comercial
        </a>
        <a
          href={getWhatsAppLink()}
          target="_blank"
          class="flex-1 rounded-2xl bg-blue-400 px-6 py-4 text-center text-lg font-black hover:bg-blue-300"
          rel="noopener noreferrer"
        >
          WhatsApp comercial
        </a>
      </div>
      <p class="mt-4 text-sm text-blue-200">
        Atencion cercana y explicacion clara de condiciones y pasos de contratacion.
      </p>
    </div>
  </div>
</section>
